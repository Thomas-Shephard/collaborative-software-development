<UserControl x:Class="Jahoot.Display.Controls.TestListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <SolidColorBrush x:Key="SecondaryTextColor" Color="Gray"/>
        <DataTemplate x:Key="TestItemTemplate">
            <StackPanel>
                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Width="40" Height="40" Background="#F0F0F0" CornerRadius="8" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Icon}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
                    </Border>

                    <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding TestName}" FontWeight="SemiBold" FontSize="16"/>
                        <TextBlock Text="{Binding SubjectName}" FontSize="12" Foreground="{StaticResource SecondaryTextColor}"/>
                        <TextBlock Text="{Binding DateInfo}" FontSize="12" Foreground="{StaticResource SecondaryTextColor}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding StatusOrScore}" 
                                   FontWeight="SemiBold"  
                                   FontSize="18"
                                   HorizontalAlignment="Right"/>
                        <TextBlock Text="{Binding StatusLabel}" 
                                   FontSize="11" 
                                   Foreground="{StaticResource SecondaryTextColor}"
                                   HorizontalAlignment="Right"
                                   Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>
                <Separator Margin="0,10"/>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    <Border Background="White" CornerRadius="8" Padding="20" Margin="0,0,10,0">
        <StackPanel>
            <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                       FontSize="18" 
                       FontWeight="SemiBold"/>
            <TextBlock Text="{Binding Subtitle, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                       Foreground="{StaticResource SecondaryTextColor}" 
                       Margin="0,2,0,15"/>

            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="400">
                <ItemsControl ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                              ItemTemplate="{StaticResource TestItemTemplate}"/>
            </ScrollViewer>
        </StackPanel>
    </Border>
</UserControl>
