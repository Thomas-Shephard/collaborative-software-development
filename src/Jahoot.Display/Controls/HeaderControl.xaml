<UserControl x:Class="Jahoot.Display.Controls.HeaderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBlock Text="{Binding Header, RelativeSource={RelativeSource AncestorType=UserControl}}" FontSize="32" FontWeight="Bold" Foreground="Black"/>
        <TextBlock Text="{Binding Subheader, RelativeSource={RelativeSource AncestorType=UserControl}}" FontSize="18" Foreground="Gray"/>
    </StackPanel>
</UserControl>
