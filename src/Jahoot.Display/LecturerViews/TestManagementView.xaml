<UserControl x:Class="Jahoot.Display.LecturerViews.TestManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Jahoot.Display.LecturerViews"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="Create New Test" HorizontalAlignment="Right" Margin="0,0,20,10" Grid.Row="0" Command="{Binding CreateTestCommand}"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Tests}" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Test Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Subject" Binding="{Binding Subject.Name}" Width="*"/>
                <DataGridTextColumn Header="Question Count" Binding="{Binding NumberOfQuestions}" Width="Auto"/>
                <DataGridTextColumn Header="Status" Binding="{Binding IsActive, Converter={StaticResource BooleanToStatusConverter}}" Width="Auto"/>
                <DataGridTextColumn Header="Attempts" Binding="{Binding Attempts}" Width="Auto"/>
                <DataGridTemplateColumn Header="Actions" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="View" Command="{Binding DataContext.ViewTestCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                <Button Content="Edit" Command="{Binding DataContext.EditTestCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                <Button Content="Delete" Command="{Binding DataContext.DeleteTestCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>


    </Grid>
</UserControl>
