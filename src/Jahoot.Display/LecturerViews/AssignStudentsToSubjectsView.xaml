<UserControl x:Class="Jahoot.Display.LecturerViews.AssignStudentsToSubjectsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Jahoot.Display.LecturerViews"
             xmlns:controls="clr-namespace:Jahoot.Display.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Border Background="#F6F7FB" Padding="20">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Subjects List -->
            <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Active Subjects" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                <controls:SubjectListControl Grid.Row="1" 
                                             ItemsSource="{Binding Subjects}" 
                                             SelectedItem="{Binding SelectedSubject, Mode=TwoWay}"
                                             ShowStatus="False"
                                             IsReadOnly="True"/>
            </Grid>
        </Border>

        <!-- Students List -->
        <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Text="Select Students to Assign" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>

                <controls:UserFeedbackBox Grid.Row="1" Margin="0,0,0,10"
                                          Message="{Binding FeedbackMessage}"
                                          IsSuccess="{Binding IsFeedbackSuccess}"/>

                <ListBox x:Name="StudentListBox" Grid.Row="2" ItemsSource="{Binding Students}" SelectionMode="Multiple" Margin="0,0,0,10" BorderThickness="1" BorderBrush="#EEE">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="5">
                                <TextBlock Text="{Binding Name}" FontWeight="Medium"/>
                                <TextBlock Text="{Binding Email}" Foreground="Gray" Margin="10,0,0,0" FontSize="11" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="5"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#E3F2FD"/>
                                    <Setter Property="BorderBrush" Value="#2196F3"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>

                <Button Grid.Row="3" Content="Assign Selected Students" 
                        Click="Assign_Click"
                        Height="40" Background="#007ACC" Foreground="White" 
                        FontWeight="Bold" BorderThickness="0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="0.9"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.5"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Border>
</UserControl>