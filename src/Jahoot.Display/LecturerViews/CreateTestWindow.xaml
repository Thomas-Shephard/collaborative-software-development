<Window x:Class="Jahoot.Display.LecturerViews.CreateTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Jahoot.Display.LecturerViews"
        Title="Create New Test"
        Height="700" Width="800"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Test Name:" Grid.Row="0"/>
        <TextBox Text="{Binding TestName, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Margin="0,25,0,0"/>

        <Label Content="Subject:" Grid.Row="1"/>
        <ComboBox ItemsSource="{Binding Subjects}" SelectedItem="{Binding SelectedSubject}" DisplayMemberPath="Name" Grid.Row="1" Margin="0,25,0,0"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
            <StackPanel>
                <Button Content="Add Question" Command="{Binding AddQuestionCommand}" Margin="0,0,0,10"/>
                <ItemsControl ItemsSource="{Binding Questions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type local:QuestionViewModel}">
                            <Border BorderBrush="LightGray" BorderThickness="1" Margin="0,5,0,5" Padding="10">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox Text="{Binding QuestionText, UpdateSourceTrigger=PropertyChanged}" 
                                                                                          Margin="0,0,5,5" Grid.Column="0"/>                                        <Button Content="Remove Question" 
                                                Command="{Binding DataContext.RemoveQuestionCommand, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                                                CommandParameter="{Binding}" 
                                                Grid.Column="1"/>
                                    </Grid>

                                    <Button Content="Add Option" Command="{Binding AddOptionCommand}" Margin="0,5,0,5"/>
                                    <ItemsControl ItemsSource="{Binding Options}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type local:QuestionOptionViewModel}">
                                                <Grid Margin="10,2,0,2">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBox Text="{Binding OptionText, UpdateSourceTrigger=PropertyChanged}" 
                                                                                                                  Grid.Column="0" Margin="0,0,5,0"/>                                                    <CheckBox Content="Correct" IsChecked="{Binding IsCorrect}" Grid.Column="1" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                                    <Button Content="Remove" 
                                                            Command="{Binding DataContext.RemoveOptionCommand, RelativeSource={RelativeSource AncestorType={x:Type local:QuestionViewModel}}}"
                                                            CommandParameter="{Binding}" 
                                                            Grid.Column="2"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="3" Margin="0,10,0,0">
            <Button Content="Save" Command="{Binding SaveCommand}" Margin="0,0,10,0"/>
            <Button Content="Discard" Command="{Binding DiscardCommand}"/>
        </StackPanel>
    </Grid>
</Window>
