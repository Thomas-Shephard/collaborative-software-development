<Window x:Class="Jahoot.Display.LecturerViews.CreateTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Jahoot.Display.LecturerViews"
        Title="Create New Test"
        Height="800" Width="900"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <SolidColorBrush x:Key="SecondaryTextColor" Color="Gray"/>
        <SolidColorBrush x:Key="RowSeparatorColor" Color="#EEEEEE"/>
        <SolidColorBrush x:Key="ButtonBorderColor" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E0E0E0"/>

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource ButtonBorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#F5F5F5"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#E0E0E0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ApproveButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#10B981"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#059669"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="RejectButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#EF4444"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#DC2626"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Border Background="White" CornerRadius="8" Padding="25" Margin="20,20,20,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,15">
                <TextBlock Text="Create New Test" FontSize="26" FontWeight="Bold" Foreground="#333333"/>
                <TextBlock Text="Define the test details, questions, and their respective options."
                           Foreground="{StaticResource SecondaryTextColor}" FontSize="14" Margin="0,2,0,0"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Margin="0,0,0,10">
                <Label Content="Test Name:"/>
                <TextBox Text="{Binding TestName, UpdateSourceTrigger=PropertyChanged}"/>

                <Label Content="Subject:"/>
                <ComboBox ItemsSource="{Binding Subjects}" SelectedItem="{Binding SelectedSubject}" DisplayMemberPath="Name"/>

                <Label Content="Number of Questions (for selection):"/>
                <TextBox Text="{Binding SelectedNumberOfQuestions, UpdateSourceTrigger=PropertyChanged}" 
                         PreviewTextInput="NumberValidationTextBox" 
                         Margin="0,0,0,10"/>
            </StackPanel>

            <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
                <StackPanel>
                    <Button Content="Add Question" Command="{Binding AddQuestionCommand}" Margin="0,0,0,10" Style="{StaticResource ApproveButtonStyle}"/>
                    <ItemsControl ItemsSource="{Binding Questions}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type local:QuestionViewModel}">
                                <Border BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Margin="0,5,0,15" Padding="15" CornerRadius="4">
                                    <StackPanel>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Label Content="Question Text:" Grid.Column="0" Margin="0,0,0,5"/>
                                            <TextBox Text="{Binding QuestionText, UpdateSourceTrigger=PropertyChanged}" 
                                                     Margin="0,0,5,5" Grid.Column="0"/>
                                            <Button Content="Remove Question" 
                                                    Command="{Binding DataContext.RemoveQuestionCommand, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                                                    CommandParameter="{Binding}" 
                                                    Grid.Column="1"
                                                    Style="{StaticResource RejectButtonStyle}"/>
                                        </Grid>

                                        <Button Content="Add Option" Command="{Binding AddOptionCommand}" Margin="0,10,0,10" Style="{StaticResource ActionButtonStyle}"/>
                                        <ItemsControl ItemsSource="{Binding Options}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate DataType="{x:Type local:QuestionOptionViewModel}">
                                                    <Border BorderBrush="{StaticResource RowSeparatorColor}" BorderThickness="0,0,0,1" Margin="0,0,0,5" Padding="0,5">
                                                        <Grid Margin="10,2,0,2">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <TextBox Text="{Binding OptionText, UpdateSourceTrigger=PropertyChanged}" 
                                                                     Grid.Column="0" Margin="0,0,5,0"/>
                                                            <CheckBox Content="Correct" IsChecked="{Binding IsCorrect}" Grid.Column="1" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                                            <Button Content="Remove" 
                                                                    Command="{Binding DataContext.RemoveOptionCommand, RelativeSource={RelativeSource AncestorType={x:Type local:QuestionViewModel}}}"
                                                                    CommandParameter="{Binding}" 
                                                                    Grid.Column="2"
                                                                    Style="{StaticResource RejectButtonStyle}"/>
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="4" Margin="0,20,0,0">
                <Button Content="Save" Command="{Binding SaveCommand}" Margin="0,0,10,0" Style="{StaticResource ApproveButtonStyle}"/>
                <Button Content="Discard" Command="{Binding DiscardCommand}" Style="{StaticResource RejectButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
