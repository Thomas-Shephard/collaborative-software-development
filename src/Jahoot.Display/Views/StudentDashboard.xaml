<UserControl x:Class="Jahoot.Display.Views.StudentDashboard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Student Dashboard" FontSize="20" FontWeight="Bold" Margin="4"/>

        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <!-- Upcoming Tests -->
            <Border Grid.Column="0" Padding="8" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4">
                <StackPanel>
                    <TextBlock Text="Upcoming Tests" FontWeight="SemiBold" Margin="0 0 0 8"/>
                    <ItemsControl ItemsSource="{Binding UpcomingTests}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="6" Margin="0 0 0 6" Background="#FAFAFA" CornerRadius="3">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding Course}" FontStyle="Italic" FontSize="11"/>
                                        <TextBlock Text="{Binding Date, StringFormat='{}{0:MMM d, yyyy}'}" FontSize="11"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- Completed Tests + Stats -->
            <Border Grid.Column="1" Padding="8" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4">
                <StackPanel>
                    <TextBlock Text="Completed Tests" FontWeight="SemiBold" Margin="0 0 0 8"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0 0 0 8">
                        <TextBlock Text="Average Score:" FontWeight="Bold" VerticalAlignment="Center"/>
                        <TextBlock Text=" " />
                        <TextBlock Text="{Binding AverageScore}" FontSize="16" Foreground="#2A7AE2" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Simple line chart area -->
                    <Border Height="140" Background="#FFF" BorderBrush="#EEE" BorderThickness="1" CornerRadius="2" Padding="6">
                        <Canvas Name="GradesCanvas" Background="Transparent" />
                    </Border>

                    <ItemsControl ItemsSource="{Binding CompletedTests}" Margin="0 8 0 0">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="6" Margin="0 0 0 6" Background="#FAFAFA" CornerRadius="3">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                                        <StackPanel Width="200">
                                            <TextBlock Text="{Binding Title}" FontWeight="Bold"/>
                                            <TextBlock Text="{Binding Course}" FontSize="11"/>
                                        </StackPanel>
                                        <TextBlock Text="{Binding Date, StringFormat='{}{0:MMM d, yyyy}'}" Width="120"/>
                                        <TextBlock Text="{Binding Score, StringFormat='{}{0:0}'}" Width="60" HorizontalAlignment="Right" FontWeight="Bold"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
