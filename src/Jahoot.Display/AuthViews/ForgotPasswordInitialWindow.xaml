<Window x:Class="Jahoot.Display.ForgotPasswordInitialWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Jahoot.Display"
        mc:Ignorable="d"
        Title="Forgot Password" Height="350" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        Background="White">
    
    <Border Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Forgot Password" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                <TextBlock Text="Enter your email address below. If your account exists, we'll send you a 6-digit code to reset your password." 
                           FontSize="14" Foreground="Gray" TextWrapping="Wrap"/>
            </StackPanel>

            <Grid Grid.Row="1" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Email Address" Style="{StaticResource FormLabel}"/>
                <TextBox x:Name="EmailTextBox" Grid.Column="1" Style="{StaticResource FormTextBox}"/>
            </Grid>

            <StackPanel Grid.Row="2">
                <Border x:Name="ErrorBanner" Background="#F8D7DA" BorderBrush="#F5C6CB" BorderThickness="1" CornerRadius="5" Margin="0,0,0,15" Padding="10" Visibility="Collapsed">
                    <TextBlock x:Name="ErrorText" Foreground="#721C24" TextWrapping="Wrap"/>
                </Border>
                
                <Border x:Name="SuccessBanner" Background="#D4EDDA" BorderBrush="#C3E6CB" BorderThickness="1" CornerRadius="5" Margin="0,0,0,15" Padding="10" Visibility="Collapsed">
                    <TextBlock x:Name="SuccessText" Foreground="#155724" TextWrapping="Wrap"/>
                </Border>
            </StackPanel>

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Cancel" Click="CancelButton_Click" Style="{StaticResource FormCancelButton}"/>
                <Button x:Name="SendCodeButton" Content="Send Code" Click="SendCodeButton_Click" Style="{StaticResource FormSaveButton}" Width="100"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
