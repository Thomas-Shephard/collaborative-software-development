<Window x:Class="Jahoot.Display.LoginPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Jahoot.Display"
        xmlns:controls="clr-namespace:Jahoot.Display.Controls"
        mc:Ignorable="d"
        Title="Jahoot - Authentication" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        MinWidth="900"
        MinHeight="750"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#007BFF"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#007BFF"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,30,0,0"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="MinHeight" Value="45"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="5">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="2" Color="Black" Opacity="0.2" BlurRadius="5"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#333333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Hyperlink">
            <Setter Property="Foreground" Value="#007BFF"/>
            <Setter Property="TextDecorations" Value="None"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="TextDecorations" Value="Underline"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <controls:HeaderControl Grid.Row="0" Margin="40,20,40,10" Header="Jahoot" Subheader="University Centre Weston - Student testing platform" />

        <Border Grid.Row="1" x:Name="LoginErrorBanner" Background="#F8D7DA" BorderBrush="#F5C6CB" BorderThickness="1" CornerRadius="5" Margin="40,0,40,10" Padding="15" Visibility="Collapsed">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="⚠" Foreground="#721C24" Margin="0,0,10,0" FontSize="14"/>
                <TextBlock x:Name="LoginErrorText" Foreground="#721C24" Text="Error message goes here" FontSize="14" FontWeight="Medium"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="#F5F5F5" CornerRadius="10" Margin="40,10,40,40">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="1" Color="Black" Opacity="0.1" BlurRadius="10"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" BorderBrush="#DDDDDD" BorderThickness="0,0,2,0" Padding="40">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0">
                            <TextBlock Text="Sign In" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBlock Text="Access your account" FontSize="16" Foreground="Gray" Margin="0,0,0,25"/>

                            <TextBlock Text="Email Address" Margin="0,0,0,5" FontWeight="Medium"/>
                            <TextBox x:Name="SignInEmailTextBox" Padding="10"/>
                            
                            <TextBlock Text="Password" Margin="0,20,0,5" FontWeight="Medium"/>
                            <PasswordBox x:Name="SignInPasswordBox" Padding="10" KeyDown="SignInPasswordBox_KeyDown"/>

                            <Grid Margin="0,20,0,0">
                                <CheckBox Content="Remember me" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <Hyperlink Click="ForgotPassword_Click">
                                        <TextBlock Text="Forgot password?"/>
                                    </Hyperlink>
                                </TextBlock>
                            </Grid>
                        </StackPanel>
                        <Button Grid.Row="2" Content="Sign In" Style="{StaticResource PrimaryButton}" Click="LoginButton_Click"/>
                    </Grid>
                </Border>

                <Border Grid.Column="1" Padding="40">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0">
                            <TextBlock Text="Create Account" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBlock Text="Register for access" FontSize="16" Foreground="Gray" Margin="0,0,0,25"/>

                            <TextBlock Text="Full Name" Margin="0,0,0,5" FontWeight="Medium"/>
                            <TextBox x:Name="RegisterFullNameTextBox" Padding="10"/>

                            <TextBlock Text="Email Address" Margin="0,20,0,5" FontWeight="Medium"/>
                            <TextBox x:Name="RegisterEmailTextBox" Padding="10"/>

                            <TextBlock Text="Password" Margin="0,20,0,5" FontWeight="Medium"/>
                            <PasswordBox x:Name="RegisterPasswordBox" Padding="10"/>
                            <ProgressBar x:Name="PasswordStrengthBar" Height="5" Margin="0,5,0,0"/>
                            <TextBlock Text="Password must be at least 8 characters long, contain a number, an uppercase and a lowercase letter." 
                                       FontSize="12" Foreground="Gray" TextWrapping="Wrap" Margin="0,5,0,0"/>
                        </StackPanel>
                        
                        <Button Grid.Row="2" Content="Register" Style="{StaticResource PrimaryButton}" Click="RegisterButton_Click"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
