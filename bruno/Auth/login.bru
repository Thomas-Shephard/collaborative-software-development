meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: http://localhost/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "{{adminemail}}",
    "password": "{{adminpassword}}"
  }
}

vars:post-response {
  auth_token: res.body.token
}
